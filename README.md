# SVG-Logo# Simple Logo Generator

## Description
This command-line application generates a simple logo with user-defined text and shape. The generated logo is saved as an SVG file.

## Usage
Run the application with:
```bash
node index.js
